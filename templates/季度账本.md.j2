# {{ 季度名称 }}账单

> 季度范围:{{ 季度范围['开始'] }} ~ {{ 季度范围['结束'] }}
> 生成日期:{{ 生成日期 }}
> 已确认 {{ 已确认交易数 }} 笔 · 待确认 {{ 待确认数 }} 笔 · 剔除 {{ 被剔除数 }} 笔

## 一、账本汇总

**总收入**:{{ '%.2f'|format(总收入) }} CNY &nbsp;&nbsp; **总支出**:{{ '%.2f'|format(总支出) }} CNY &nbsp;&nbsp; **净结余**:{{ '%.2f'|format(净结余) }} CNY

| 支出类别 | 金额 | 占比 |
|---|---|---|
{% for c in 支出类别占比 %}| {{ c['类别'] }} | {{ '%.2f'|format(c['金额']) }} | {{ '%.1f'|format(c['占比']) }}% |
{% else %}| (本季度无支出) | — | — |
{% endfor %}

## 二、交易记录

| 日期 | 平台/来源文件 | 类别 | 收支类型 | 金额 | 币种 | 状态 | 备注 |
|---|---|---|---|---|---|---|---|
{% for t in 交易记录|sort(attribute='日期', reverse=true) %}| {{ t['日期'] }} | {{ t['平台'] }} | {{ t['类别'] }}{% if t['待确认'] %}(待确认){% endif %} | {{ '收入' if t['金额'] > 0 else '支出' }} | {{ '%.2f'|format(t['金额']) }} | {{ t['币种'] }} | {{ t['状态'] }} | {{ t['备注'] }} |
{% endfor %}

## 三、已剔除记录(去重 / 内部转账 / 信用卡还款 / 退款)

| 日期 | 平台/来源文件 | 金额 | 币种 | 剔除原因 |
|---|---|---|---|---|
{% for r in 已剔除记录 %}| {{ r['日期'] }} | {{ r['平台'] }} | {{ '%.2f'|format(r['金额']) }} | {{ r['币种'] }} | {{ r['原因'] }} |
{% else %}| (本季度无剔除记录) | — | — | — | — |
{% endfor %}

## 四、待确认队列

| 日期 | 平台/来源文件 | 金额 | 币种 | 无法确认的原因 | 需要向我确认的问题 |
|---|---|---|---|---|---|
{% for t in 交易记录|selectattr('待确认')|sort(attribute='金额') %}| {{ t['日期'] }} | {{ t['平台'] }} | {{ '%.2f'|format(t['金额']) }} | {{ t['币种'] }} | 类别无法自动确定 | 这笔属于什么消费或收入? |
{% endfor %}

---

## 记录规则

- 季度账本为全局账本的季度切片,UI 与数据口径与全局账本一致,仅范围为本季度
- 待确认交易计入"四、待确认队列",确认后用 global_ledger.py 的 `--confirm` 写入确认记录
- 所有金额注明币种、数据日期、来源文件;不擅自换算汇率
